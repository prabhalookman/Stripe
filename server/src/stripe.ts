// // Keep this stripe in separate file to use it as globally in other places
// import * as Stripe from 'stripe';
// //console.log("process.env.STRIPE_SCECRET : ", process.env.STRIPE_SCECRET);
// export const stripe = new Stripe(process.env.STRIPE_SCECRET!);
